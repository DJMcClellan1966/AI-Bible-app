<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:AI_Bible_App.Maui.ViewModels"
             xmlns:models="clr-namespace:AI_Bible_App.Core.Models;assembly=AI-Bible-App.Core"
             xmlns:controls="clr-namespace:AI_Bible_App.Maui.Controls"
             x:Class="AI_Bible_App.Maui.Views.ChatPage"
             x:DataType="vm:ChatViewModel"
             x:Name="ThisPage"
             Title="{Binding Title}"
             BackgroundColor="{DynamicResource PageBackgroundColor}">
    
    <Grid RowDefinitions="*,Auto,Auto">
        
        <!-- Chat Messages -->
        <ScrollView Grid.Row="0" x:Name="ChatScrollView">
            <CollectionView ItemsSource="{Binding Messages}"
                           Margin="15">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:ChatMessage">
                        <Grid Margin="0,5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- User Message (Right Aligned) -->
                            <Border Grid.Row="0"
                                    IsVisible="{Binding Role, Converter={StaticResource StringEqualConverter}, ConverterParameter='user'}"
                                    HorizontalOptions="End"
                                    MaximumWidthRequest="300"
                                    Padding="15"
                                    BackgroundColor="{DynamicResource Primary}">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="20"/>
                                </Border.StrokeShape>
                                <Label Text="{Binding Content}"
                                       TextColor="White"
                                       LineBreakMode="WordWrap"/>
                            </Border>
                            
                            <!-- Assistant Message (Left Aligned) -->
                            <Border Grid.Row="0"
                                    IsVisible="{Binding Role, Converter={StaticResource StringEqualConverter}, ConverterParameter='assistant'}"
                                    HorizontalOptions="Start"
                                    MaximumWidthRequest="300"
                                    Padding="15"
                                    StrokeThickness="1"
                                    Stroke="{DynamicResource Gray300}"
                                    BackgroundColor="{DynamicResource CardBackground}">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="20"/>
                                </Border.StrokeShape>
                                <VerticalStackLayout Spacing="8">
                                    <controls:BibleLinkLabel LinkedText="{Binding Content}"
                                           TextColor="{DynamicResource TextColor}"
                                           LineBreakMode="WordWrap"/>
                                    <!-- Rating, Speak and Save buttons -->
                                    <HorizontalStackLayout Spacing="4" HorizontalOptions="End">
                                        <!-- Thumbs Up -->
                                        <ImageButton Source="thumbs_up.png"
                                                     WidthRequest="32"
                                                     HeightRequest="32"
                                                     BackgroundColor="{AppThemeBinding Light=#F0F0F0, Dark=#404040}"
                                                     CornerRadius="16"
                                                     Padding="4"
                                                     Opacity="{Binding Rating, Converter={StaticResource RatingOpacityConverter}, ConverterParameter='1'}"
                                                     Command="{Binding Path=BindingContext.RateMessageCommand, Source={x:Reference ThisPage}}"
                                                     CommandParameter="{Binding ., Converter={StaticResource MessageToRatingTupleConverter}, ConverterParameter='1'}">
                                            <ImageButton.Source>
                                                <FontImageSource Glyph="👍" FontFamily="SegoeUI" Size="18" Color="{AppThemeBinding Light=#333333, Dark=#CCCCCC}"/>
                                            </ImageButton.Source>
                                        </ImageButton>
                                        <!-- Thumbs Down -->
                                        <ImageButton WidthRequest="32"
                                                     HeightRequest="32"
                                                     BackgroundColor="{AppThemeBinding Light=#F0F0F0, Dark=#404040}"
                                                     CornerRadius="16"
                                                     Padding="4"
                                                     Opacity="{Binding Rating, Converter={StaticResource RatingOpacityConverter}, ConverterParameter='-1'}"
                                                     Command="{Binding Path=BindingContext.RateMessageCommand, Source={x:Reference ThisPage}}"
                                                     CommandParameter="{Binding ., Converter={StaticResource MessageToRatingTupleConverter}, ConverterParameter='-1'}">
                                            <ImageButton.Source>
                                                <FontImageSource Glyph="👎" FontFamily="SegoeUI" Size="18" Color="{AppThemeBinding Light=#333333, Dark=#CCCCCC}"/>
                                            </ImageButton.Source>
                                        </ImageButton>
                                        <!-- Speak -->
                                        <ImageButton WidthRequest="32"
                                                     HeightRequest="32"
                                                     BackgroundColor="{AppThemeBinding Light=#F0F0F0, Dark=#404040}"
                                                     CornerRadius="16"
                                                     Padding="4"
                                                     ToolTipProperties.Text="Read aloud"
                                                     Command="{Binding Path=BindingContext.ToggleVoiceCommand, Source={x:Reference ThisPage}}"
                                                     CommandParameter="{Binding}">
                                            <ImageButton.Source>
                                                <FontImageSource Glyph="🔊" FontFamily="SegoeUI" Size="18" Color="{AppThemeBinding Light=#333333, Dark=#CCCCCC}"/>
                                            </ImageButton.Source>
                                        </ImageButton>
                                        <!-- Save -->
                                        <ImageButton WidthRequest="32"
                                                     HeightRequest="32"
                                                     BackgroundColor="{AppThemeBinding Light=#F0F0F0, Dark=#404040}"
                                                     CornerRadius="16"
                                                     Padding="4"
                                                     ToolTipProperties.Text="Save to reflections"
                                                     Command="{Binding Path=BindingContext.SaveToReflectionsCommand, Source={x:Reference ThisPage}}"
                                                     CommandParameter="{Binding}">
                                            <ImageButton.Source>
                                                <FontImageSource Glyph="📝" FontFamily="SegoeUI" Size="18" Color="{AppThemeBinding Light=#333333, Dark=#CCCCCC}"/>
                                            </ImageButton.Source>
                                        </ImageButton>
                                        <!-- Prayer -->
                                        <ImageButton WidthRequest="32"
                                                     HeightRequest="32"
                                                     BackgroundColor="{AppThemeBinding Light=#F0F0F0, Dark=#404040}"
                                                     CornerRadius="16"
                                                     Padding="4"
                                                     ToolTipProperties.Text="Generate a prayer"
                                                     Command="{Binding Path=BindingContext.GeneratePrayerFromMessageCommand, Source={x:Reference ThisPage}}"
                                                     CommandParameter="{Binding}">
                                            <ImageButton.Source>
                                                <FontImageSource Glyph="🙏" FontFamily="SegoeUI" Size="18" Color="{AppThemeBinding Light=#333333, Dark=#CCCCCC}"/>
                                            </ImageButton.Source>
                                        </ImageButton>
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                            </Border>

                            <!-- Contextual References (shown below assistant messages) -->
                            <Border Grid.Row="1"
                                    IsVisible="{Binding HasContextualReferences}"
                                    HorizontalOptions="Start"
                                    MaximumWidthRequest="320"
                                    Margin="0,5,0,0"
                                    Padding="12"
                                    StrokeThickness="1"
                                    Stroke="{DynamicResource Primary}"
                                    BackgroundColor="{DynamicResource CardBackground}">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12"/>
                                </Border.StrokeShape>
                                <VerticalStackLayout Spacing="8">
                                    <HorizontalStackLayout Spacing="5">
                                        <Label Text="📖" FontSize="14"/>
                                        <Label Text="Related Scripture from my experience:"
                                               FontSize="12"
                                               FontAttributes="Bold"
                                               TextColor="{DynamicResource Primary}"/>
                                    </HorizontalStackLayout>
                                    
                                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding ContextualReferences}" Spacing="10">
                                        <BindableLayout.ItemTemplate>
                                            <DataTemplate x:DataType="models:ContextualReference">
                                                <VerticalStackLayout Spacing="3">
                                                    <controls:BibleLinkLabel LinkedText="{Binding Reference}"
                                                                             FontSize="13"
                                                                             FontAttributes="Bold"
                                                                             TextColor="{DynamicResource Primary}"/>
                                                    <Label Text="{Binding Summary}"
                                                           FontSize="12"
                                                           TextColor="{DynamicResource TextColor}"
                                                           LineBreakMode="WordWrap"/>
                                                    <Label Text="{Binding Connection, StringFormat='💬 \&quot;{0}\&quot;'}"
                                                           FontSize="11"
                                                           FontAttributes="Italic"
                                                           TextColor="{DynamicResource Gray600}"
                                                           LineBreakMode="WordWrap"/>
                                                </VerticalStackLayout>
                                            </DataTemplate>
                                        </BindableLayout.ItemTemplate>
                                    </VerticalStackLayout>
                                </VerticalStackLayout>
                            </Border>

                            <!-- System Message (Centered, Error) -->
                            <Border Grid.Row="0"
                                    IsVisible="{Binding Role, Converter={StaticResource StringEqualConverter}, ConverterParameter='system'}"
                                    HorizontalOptions="Center"
                                    Padding="10"
                                    BackgroundColor="{DynamicResource Warning}">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8"/>
                                </Border.StrokeShape>
                                <Label Text="{Binding Content}"
                                       FontSize="12"
                                       TextColor="White"
                                       LineBreakMode="WordWrap"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>

        <!-- Typing Indicator (Row 1 - between messages and input) -->
        <Border Grid.Row="1"
                IsVisible="{Binding IsAiTyping}"
                HorizontalOptions="Start"
                Margin="15,8"
                Padding="12,8"
                BackgroundColor="{DynamicResource CardBackground}"
                StrokeThickness="1"
                Stroke="{DynamicResource Gray300}">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="15"/>
            </Border.StrokeShape>
            <HorizontalStackLayout Spacing="8">
                <ActivityIndicator IsRunning="True"
                                 WidthRequest="16"
                                 HeightRequest="16"
                                 Color="{DynamicResource Primary}"/>
                <Label Text="{Binding Character.Name, StringFormat='{0} is typing...'}"
                       FontSize="13"
                       FontAttributes="Italic"
                       TextColor="{DynamicResource Gray600}"/>
            </HorizontalStackLayout>
        </Border>

        <!-- Input Area -->
        <Grid Grid.Row="2" 
              ColumnDefinitions="*,Auto"
              Padding="15,10"
              BackgroundColor="{DynamicResource CardBackground}">
            
            <Border Grid.Column="0"
                    Padding="10"
                    StrokeThickness="1"
                    Stroke="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray600}}"
                    BackgroundColor="{AppThemeBinding Light={StaticResource InputBackground}, Dark={StaticResource InputBackgroundDark}}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="25"/>
                </Border.StrokeShape>
                <Entry Text="{Binding UserMessage}"
                       Placeholder="Type a message..."
                       ReturnCommand="{Binding SendMessageCommand}"
                       IsEnabled="{Binding IsAiTyping, Converter={StaticResource InvertedBoolConverter}}"
                       TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                       PlaceholderColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                       VerticalOptions="Center"/>
            </Border>

            <!-- Send Button (visible when not typing) -->
            <Button Grid.Column="1"
                    Text="Send"
                    Command="{Binding SendMessageCommand}"
                    IsVisible="{Binding IsAiTyping, Converter={StaticResource InvertedBoolConverter}}"
                    Margin="10,0,0,0"
                    WidthRequest="80"
                    HeightRequest="50"
                    CornerRadius="25"
                    BackgroundColor="{DynamicResource Primary}"
                    TextColor="White"/>
            
            <!-- Stop Button (visible when AI is typing) -->
            <Button Grid.Column="1"
                    Text="⏹ Stop"
                    Command="{Binding CancelResponseCommand}"
                    IsVisible="{Binding IsAiTyping}"
                    Margin="10,0,0,0"
                    WidthRequest="80"
                    HeightRequest="50"
                    CornerRadius="25"
                    FontSize="14"
                    BackgroundColor="#E74C3C"
                    TextColor="White"/>
        </Grid>

    </Grid>
</ContentPage>
